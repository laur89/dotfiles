// from https://wiki.debian.org/UnattendedUpgrades / https://wiki.debian.org/PeriodicUpdates
// to be placed in /etc/apt/apt.conf.d
//
// to manually debug the config, run  $ sudo unattended-upgrade -d
//
// note the used sources are set by the [Unattended-Upgrade::Origins-Pattern] bit,
// likely in /etc/apt/apt.conf.d/50unattended-upgrades
//
// this config is likely used by /usr/lib/apt/apt.systemd.daily that's
// executed by systemd, see:  $ systemctl show apt-daily-upgrade.service
//
// note https://wiki.debian.org/DebianUnstable#What_are_some_best_practices_for_testing.2Fsid_users.3F
// recommends disabling unattended upgrades:
// > Consider (especially when using unstable) if you need to disable or remove
//   unattended-upgrades in order to control when package updates take place
///////////////////////////////////////////////////////////
// Control parameters for cron jobs by /etc/cron.daily/apt-compat //


// Enable the update/upgrade script (0=disable)
APT::Periodic::Enable "1";


// Do "apt-get update" automatically every n-days (0=disable)
APT::Periodic::Update-Package-Lists "1";


// Do "apt-get upgrade --download-only" every n-days (0=disable)
APT::Periodic::Download-Upgradeable-Packages "1";


// Run the "unattended-upgrade" security upgrade script
// every n-days (0=disabled)
// Requires the package "unattended-upgrades" and will write
// a log in /var/log/unattended-upgrades
APT::Periodic::Unattended-Upgrade "1";


// Do "apt-get autoclean" every n-days (0=disable)
APT::Periodic::AutocleanInterval "21";


// Send report mail to root
//     0:  no report             (or null string)
//     1:  progress report       (actually any string)
//     2:  + command outputs     (remove -qq, remove 2>/dev/null, add -d)
//     3:  + trace on
APT::Periodic::Verbose "2";
