[Command]
Command="
    copyq:
    var shortcut = str(data(mimeShortcut))
    var row = selectedItems()[0]
    if (shortcut == 'ctrl+j')
        selectItems(row + 1)
    else
        selectItems(row - 1)"
Icon=\xf338
InMenu=true
Name=Up/Down
Shortcut=ctrl+j, ctrl+k
