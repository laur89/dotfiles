# path to the fasd data file; defaults to ~/.fasd
_FASD_DATA="$XDG_DATA_HOME/fasd/data"

# path to .viminfo file for viminfo backend
_FASD_VIMINFO="$HOME/.local/share/nvim/shada/main.shada"

# resolve symlinks to prevet duplicates (requires whjvenyl/fasd):
_FASD_RESOLVE_SYMLINKS=1

# for backends manual see https://github.com/whjvenyl/fasd#backends
# fasd backends to use; note 'native' is the default:
_FASD_BACKENDS='native current recently-used viminfo'

# force case insensitivity:
_FASD_NOCASE=1

# commands to ignore: (defaults to [fasd ls echo] but might've changed)
_FASD_IGNORE="fasd ls echo z zz e d goto gt find ffind fzf fd gito fo gg fog foa fon fow foc ffstr ag rg grep fh fhd history lgrep igrep latr lat lt ltr eza err report homeshick dd git rm mv"

# whether $PWD entry should be normalized to root of VCS, when in one; defaults to 1:
#_FASD_ONLY_VCS=0

# enable logging:
#_FASD_SINK="$HOME/.fasd.log"
